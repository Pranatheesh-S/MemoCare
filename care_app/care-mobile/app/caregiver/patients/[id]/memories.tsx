import { Alert, Pressable, Text } from "react-native"; import * as ImagePicker from "expo-image-picker"; import { Screen } from "@/Screen"; import { colors, styles } from "@/theme";
export default function Memories() { async function upload() { const permission = await ImagePicker.requestMediaLibraryPermissionsAsync(); if (!permission.granted) { Alert.alert("Permission needed", "Memory Vault needs photo access."); return; } const result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ["images"], quality: 0.8 }); if (!result.canceled) Alert.alert("Ready to upload", "The selected memory is queued for secure upload."); } return <Screen title="Memory Vault"><Text style={styles.muted}>Only upload photos with consent from everyone pictured.</Text><Pressable style={styles.button} onPress={upload}><Text style={styles.buttonText}>Choose a photo</Text></Pressable><Text style={styles.card}>Bihu celebration at home{"\n"}<Text style={{ color: colors.muted }}>Majuli</Text></Text></Screen>; }
